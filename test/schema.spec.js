const request = require('request-promise');
const {
  beforeAll,
  afterAll,
  describe,
  expect,
  it,
} = require('./bootstrap');
const schema = require('../src/schema');

// describe('schema', () => {
//   it('should create an')
// });
